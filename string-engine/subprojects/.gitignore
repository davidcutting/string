# Only wrap definitions and our packagefiles are tracked; everything else here
# (extracted sources, packagecache, wrapdb.json, .wraplock) is regenerated by
# `meson setup` and must not be committed.
*
!.gitignore
!*.wrap
!packagefiles
!packagefiles/**
